# Email Configuration - Using Brevo SMTP
EMAIL_HOST=smtp-relay.brevo.com
EMAIL_PORT=587
EMAIL_SECURE=false
EMAIL_USER=your_smtp_user@smtp-brevo.com
EMAIL_PASS=your_smtp_password_here
EMAIL_TO=team@spectrity.bio
EMAIL_FROM=connect@spectrity.bio

# R2 Storage Configuration
CLOUDFLARE_ACCOUNT_ID=your_cloudflare_account_id
R2_ACCESS_KEY_ID=your_r2_access_key
R2_SECRET_ACCESS_KEY=your_r2_secret_key

# For Gmail, you need to use an App Password:
# 1. Go to Google Account settings
# 2. Security > 2-Step Verification (must be enabled)
# 3. App passwords > Generate new app password
# 4. Use the generated password for EMAIL_PASS

# For other email services:
# - Outlook: use "hotmail" as EMAIL_SERVICE
# - Yahoo: use "yahoo" as EMAIL_SERVICE
# - Custom SMTP: Remove EMAIL_SERVICE and use:
#   EMAIL_HOST=smtp.example.com
#   EMAIL_PORT=587
#   EMAIL_SECURE=false